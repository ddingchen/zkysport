var arr = new Array;//场地号预定var n=0;//场地数量//场地号选择//页面各高度自适应function gaodu() {    var width = $(window).width();    var bili = 1 / 750 * width;//页面比例    var ziti = 10 * bili;//字体自适应参数    var xian = 4 * bili;//边框    $("html").css("font-size", ziti + "px");    $(".gekai").css("margin-left", '6.5%');    $(".xian").css("width", '100%');}$(function () {    gaodu();    $('.changdi_x div').on("touchstart", function () {        if ($(this).hasClass("kong")) {            var id = $(this).attr("data-id");            $(this).removeClass('kong');            $(this).addClass('xuan');            arr[id] = id;//array_push()            n++;        } else if ($(this).hasClass("xuan")) {            var id = $(this).attr("data-id");            $(this).removeClass('xuan');            $(this).addClass('kong');            arr[id] = null;            n--;        }        var z = JSON.stringify(arr);        z = z.replace(/null,/g, "");        z = z.replace(/,null/g, "");        $("#inp").val(z);    });});//场地未选报错函数function click(xx) {    if (n <= 0) {        alert("请选择至少1块场地,或者点击左上角返回");        return false;    } else {        location.href = "changguanyuding.html?n=" + xx;//location.href实现客户端页面的跳转        return true;    }}//场地选择切换页面函数function hhhhhhhhh() {    if (leixing == 1) {        $('.ymq').show();        $(".ppq").hide();        $(".xian").show();    } else if (leixing == 2) {        $('.ppq').show();        $(".ymq").hide();        $(".xian").hide();    }}